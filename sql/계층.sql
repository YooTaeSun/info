SELECT
	EMPNO,
	ENAME,
	LEVEL,
	MGR,
	PRIOR ENAME AS MGR_ENAME,
	SUBSTR( SYS_CONNECT_BY_PATH( ENAME, '-' ), 2 )
FROM
	EMP
START WITH
	MGR IS NULL
CONNECT BY
	PRIOR EMPNO = MGR
ORDER SIBLINGS BY EMPNO;
